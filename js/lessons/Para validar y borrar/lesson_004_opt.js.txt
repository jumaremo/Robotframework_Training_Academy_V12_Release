/**
 * Robot Framework Academy - Lesson 004 OPTIMIZED
 * Plugins esenciales para PyCharm
 */

const LESSON_004 = {
    id: 4,
    title: "Plugins esenciales para PyCharm",
    duration: "5 min",
    level: "beginner",
    section: "section-01",
    type: "standard", // ✅ AGREGADO
    content: `
        <h2>🔌 Plugins PyCharm</h2>
        <p>Plugins transforman PyCharm en máquina productividad RF.</p>
        
        <h3>🤖 Tests plugins:</h3>
        <pre><code class="robot">*** Variables ***
\${PLUGIN_RF}        Robot Framework
\${PLUGIN_PYTHON}    Python Community Edition
\${PLUGIN_YAML}      YAML/Ansible
\${PLUGIN_GIT}       Git
\${PLUGIN_TERMINAL}  Terminal
\${STATUS_OK}        installed
\${VERSION_CHECK}    --version

*** Test Cases ***
Verificar Plugin Robot Framework
    Log                 🔍 Verificando plugin RF
    \${plugin_status}=   Set Variable    \${STATUS_OK}
    Should Be Equal     \${plugin_status}    \${STATUS_OK}
    Should Contain      \${PLUGIN_RF}    Robot
    Log                 ✅ Plugin \${PLUGIN_RF} OK
    Should Be True      True

Verificar Plugin Python
    Log                 🐍 Verificando Python plugin
    \${python_plugin}=   Set Variable    enabled
    Should Be Equal     \${python_plugin}    enabled
    Should Contain      \${PLUGIN_PYTHON}    Python
    Log                 ✅ \${PLUGIN_PYTHON} habilitado
    Should Be True      True

Verificar Plugin YAML
    Log                 📄 Verificando YAML support
    \${yaml_support}=    Set Variable    active
    Should Be Equal     \${yaml_support}    active
    Should Contain      \${PLUGIN_YAML}    YAML
    Log                 ✅ \${PLUGIN_YAML} funcionando
    Should Be True      True

Verificar Plugin Git
    Log                 🔧 Verificando Git integration
    \${git_plugin}=      Set Variable    ready
    Should Be Equal     \${git_plugin}    ready
    Should Contain      \${PLUGIN_GIT}    Git
    Log                 ✅ \${PLUGIN_GIT} integrado
    Should Be True      True

Verificar Plugin Terminal
    Log                 💻 Verificando Terminal
    \${terminal_ready}=  Set Variable    available
    Should Be Equal     \${terminal_ready}    available
    Should Contain      \${PLUGIN_TERMINAL}    Terminal
    Log                 ✅ \${PLUGIN_TERMINAL} listo
    Should Be True      True

Test Funcionalidad Plugins
    Log                 🎯 Testing funcionalidad
    \${all_plugins}=     Set Variable    \${PLUGIN_RF}, \${PLUGIN_PYTHON}
    Should Contain      \${all_plugins}    Robot
    Should Contain      \${all_plugins}    Python
    Log                 ✅ Todos plugins funcionando
    Should Be True      True
    Should Not Be Empty \${all_plugins}</code></pre>
        
        <h3>🎯 Práctica plugins (4 min):</h3>
        <p>1. Abre PyCharm → File → Settings → Plugins</p>
        <p>2. Click Marketplace → Busca "Robot Framework"</p>
        <p>3. Instala plugin "Robot Framework" → Install</p>
        <p>4. Verifica "Python Community Edition" enabled</p>
        <p>5. Busca e instala "YAML/Ansible" plugin</p>
        <p>6. Confirma "Git" plugin enabled por defecto</p>
        <p>7. Habilita "Terminal" plugin si no está</p>
        <p>8. Click "Apply" → "OK" → Restart PyCharm</p>
        <p>9. Crea archivo "test.robot" → Verifica colores syntax</p>
        <p>10. Escribe "Open Bro" → Ve autocompletado "Open Browser"</p>
        <p>11. Right-click test.robot → Ve opción "Run"</p>
        <p>12. Abre Terminal: View → Tool Windows → Terminal</p>
        <p>13. Ejecuta: robot --version → Verifica output</p>
        <p>14. Settings → Keymap → Asigna F5 a "Run"</p>
        <p>15. Crea config.yaml → Verifica syntax highlighting</p>

        <h3>💡 Tip</h3>
        <p>Plugin "Robot Framework" es esencial para syntax highlighting</p>
        
        <h3>🚀 Siguiente</h3>
        <p>Lección 005: Creación primer proyecto</p>
    `,
    topics: ["plugins", "productividad"],
    hasCode: true,
    hasExercise: true,
    type: "standard"
};