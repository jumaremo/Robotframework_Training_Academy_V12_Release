/**
 * Robot Framework Academy - Lesson 015 OPTIMIZED
 * Troubleshooting de instalación común
 */

const LESSON_015 = {
    id: 15,
    title: "Troubleshooting de instalación común",
    duration: "8 min",
    level: "beginner",
    section: "section-01",
    type: "foundation",
    content: `
        <h2>🔧 Troubleshooting Instalación</h2>
        <p>Resolver problemas RF = skill #1 QA Engineer.</p>
        
        <h3>🤖 Tests diagnóstico:</h3>
        <pre><code class="robot">*** Variables ***
\${PYTHON_VERSION}       3.9.0
\${PYTHON_PATH}          C:\\\\Python39\\\\python.exe
\${RF_VERSION}           6.1.1
\${RF_COMMAND}           robot --version
\${SELENIUM_VERSION}     6.2.0
\${WEBDRIVER_PATH}       C:\\\\webdrivers\\\\chromedriver.exe
\${PATH_PYTHON}          C:\\\\Python39\\\\Scripts
\${PATH_WEBDRIVER}       C:\\\\webdrivers
\${PYCHARM_INTERPRETER}  C:\\\\Python39\\\\python.exe
\${RF_PLUGIN}            Robot Framework Support

*** Test Cases ***
Test Python Installation
    Log                    🐍 Testing Python installation
    Should Not Be Empty    \${PYTHON_VERSION}
    Should Contain         \${PYTHON_VERSION}    3.
    Should Be True         len('\${PYTHON_VERSION}') >= 5
    Should Not Be Empty    \${PYTHON_PATH}
    Should Contain         \${PYTHON_PATH}    python.exe
    Should Contain         \${PYTHON_PATH}    Python
    Should Be Equal        \${PYTHON_VERSION}    3.9.0
    Should Be Equal        \${PYTHON_PATH}    C:\\\\Python39\\\\python.exe
    Should Not Be Empty    3.9.0
    Should Not Be Empty    C:\\\\Python39\\\\python.exe
    Log                    ✅ Python installation OK

Test Robot Framework Installation
    Log                    🤖 Testing Robot Framework
    Should Not Be Empty    \${RF_VERSION}
    Should Contain         \${RF_VERSION}    6.
    Should Be True         len('\${RF_VERSION}') >= 5
    Should Not Be Empty    \${RF_COMMAND}
    Should Contain         \${RF_COMMAND}    robot
    Should Contain         \${RF_COMMAND}    --version
    Should Be Equal        \${RF_VERSION}    6.1.1
    Should Be Equal        \${RF_COMMAND}    robot --version
    Should Not Be Empty    6.1.1
    Should Not Be Empty    robot --version
    Log                    ✅ Robot Framework OK

Test SeleniumLibrary Installation
    Log                    🌐 Testing SeleniumLibrary
    Should Not Be Empty    \${SELENIUM_VERSION}
    Should Contain         \${SELENIUM_VERSION}    6.
    Should Be True         len('\${SELENIUM_VERSION}') >= 5
    Should Not Be Empty    \${WEBDRIVER_PATH}
    Should Contain         \${WEBDRIVER_PATH}    chromedriver.exe
    Should Contain         \${WEBDRIVER_PATH}    webdrivers
    Should Be Equal        \${SELENIUM_VERSION}    6.2.0
    Should Be Equal        \${WEBDRIVER_PATH}    C:\\\\webdrivers\\\\chromedriver.exe
    Should Not Be Empty    6.2.0
    Should Not Be Empty    C:\\\\webdrivers\\\\chromedriver.exe
    Log                    ✅ SeleniumLibrary OK

Test PATH Environment
    Log                    🔧 Testing PATH environment
    Should Not Be Empty    \${PATH_PYTHON}
    Should Contain         \${PATH_PYTHON}    Python39
    Should Contain         \${PATH_PYTHON}    Scripts
    Should Not Be Empty    \${PATH_WEBDRIVER}
    Should Contain         \${PATH_WEBDRIVER}    webdrivers
    Should Be Equal        \${PATH_PYTHON}    C:\\\\Python39\\\\Scripts
    Should Be Equal        \${PATH_WEBDRIVER}    C:\\\\webdrivers
    Should Not Be Empty    C:\\\\Python39\\\\Scripts
    Should Not Be Empty    C:\\\\webdrivers
    Log                    ✅ PATH environment OK

Test PyCharm Integration
    Log                    💻 Testing PyCharm integration
    Should Not Be Empty    \${PYCHARM_INTERPRETER}
    Should Contain         \${PYCHARM_INTERPRETER}    python.exe
    Should Not Be Empty    \${RF_PLUGIN}
    Should Contain         \${RF_PLUGIN}    Robot Framework
    Should Contain         \${RF_PLUGIN}    Support
    Should Be Equal        \${PYCHARM_INTERPRETER}    C:\\\\Python39\\\\python.exe
    Should Be Equal        \${RF_PLUGIN}    Robot Framework Support
    Should Not Be Empty    C:\\\\Python39\\\\python.exe
    Should Not Be Empty    Robot Framework Support
    Log                    ✅ PyCharm integration OK

Test Common Errors Solutions
    Log                    🔒 Testing error solutions
    Should Contain         Permission Denied    Permission
    Should Contain         Run as Administrator    Administrator
    Should Contain         ModuleNotFoundError    Module
    Should Contain         pip install robotframework    install
    Should Contain         WebDriverException    WebDriver
    Should Contain         Download latest ChromeDriver    ChromeDriver
    Should Not Be Empty    Permission Denied
    Should Not Be Empty    Run as Administrator
    Should Not Be Empty    ModuleNotFoundError
    Should Not Be Empty    pip install robotframework
    Should Be Equal        Permission Denied    Permission Denied
    Should Be Equal        ModuleNotFoundError    ModuleNotFoundError
    Log                    ✅ Error solutions validated

Test PATH Fix Solutions
    Log                    🛠️ Testing PATH fix solutions
    Should Contain         System Properties    Properties
    Should Contain         Environment Variables    Variables
    Should Contain         Add Python paths    Python
    Should Contain         Add to PATH    PATH
    Should Not Be Empty    System Properties
    Should Not Be Empty    Environment Variables
    Should Not Be Empty    Add Python paths
    Should Not Be Empty    Add to PATH
    Should Be Equal        System Properties    System Properties
    Should Be Equal        Environment Variables    Environment Variables
    Should Be Equal        Add Python paths    Add Python paths
    Should Be Equal        Add to PATH    Add to PATH
    Log                    ✅ PATH fixes validated

Test Installation Commands
    Log                    📦 Testing installation commands
    Should Contain         python --version    python
    Should Contain         robot --version    robot
    Should Contain         pip list    pip
    Should Contain         pip install robotframework    robotframework
    Should Contain         pip uninstall robotframework    uninstall
    Should Contain         pip install --upgrade robotframework    upgrade
    Should Not Be Empty    python --version
    Should Not Be Empty    robot --version
    Should Not Be Empty    pip list
    Should Not Be Empty    pip install robotframework
    Should Be Equal        python --version    python --version
    Should Be Equal        robot --version    robot --version
    Log                    ✅ Installation commands validated</code></pre>
        
        <h3>🎯 Troubleshooting práctico (6 min):</h3>
        <p>1. Check Python: python --version → Debe mostrar Python 3.x</p>
        <p>2. Check Robot Framework: robot --version → Verificar versión</p>
        <p>3. Check pip packages: pip list | grep robot → Ver paquetes RF</p>
        <p>4. Fix Permission Denied: Run CMD as Administrator → Retry install</p>
        <p>5. Fix Module Not Found: pip install robotframework → Reinstalar</p>
        <p>6. Fix PATH: System Properties → Environment Variables → Add Python</p>
        <p>7. Download ChromeDriver: chromedriver.chromium.org → Latest version</p>
        <p>8. Fix WebDriver PATH: Copy chromedriver.exe → Add to PATH</p>
        <p>9. Virtual environment: python -m venv venv → venv\\Scripts\\activate</p>
        <p>10. Reinstall clean: pip uninstall robotframework → pip install</p>
        <p>11. PyCharm interpreter: Settings → Python Interpreter → Select Python</p>
        <p>12. Install RF plugin: Settings → Plugins → "Robot Framework Support"</p>
        <p>13. Test basic: Create test.robot → robot test.robot → Verify</p>
        <p>14. Test SeleniumLibrary: python -c "import SeleniumLibrary" → No errors</p>
        <p>15. Test browser: Simple Open Browser test → Verify Chrome opens</p>
        <p>16. Clear cache: pip cache purge → Clean pip cache</p>
        <p>17. Update packages: pip install --upgrade robotframework → Latest</p>
        <p>18. Check firewall: Windows Defender → Allow Python/chromedriver</p>

        <h3>💡 Tip</h3>
        <p>90% problemas = PATH mal configurado. Run as Administrator</p>
        
        <h3>🚀 Siguiente</h3>
        <p>Lección 016: Sintaxis básica Robot Framework</p>
    `,
    topics: ["troubleshooting", "problems", "solutions"],
    hasCode: true,
    hasExercise: true,
    type: "foundation"
};